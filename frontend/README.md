# TeleCRM Frontend

Quick start:

1. Install dependencies

```bash
cd frontend
npm install
```

2. Run dev server

```bash
npm run dev
```

The frontend expects the backend to be available at the same host under `/api` (relative requests). Adjust `src/services/api.js` if needed.
